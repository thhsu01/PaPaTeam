# 領域語彙

本檔定義爬爬小隊網站的統一名詞。**動任何頁面或共用檔之前先讀這裡**——名詞不一致的代價
在這個專案已經發生過好幾次（同一個概念在五頁有五種顏色、同一個地點在兩處里程不同）。

規範與版面規則在 `ARCHITECTURE.md`，資料慣例在 `manifest.json` 的 `conventions`，
承重的決策記在 `docs/adr/`。本檔只管**詞怎麼用**。

---

## 行程

一次登山活動。有三種狀態，決定它出現在首頁哪一區、以及詳情頁的第五段叫什麼：

| 狀態 | 首頁區塊 | 導覽鍵／`<h2>` |
|---|---|---|
| 計畫 | `data.planned` | 預估進度／預計行程進度 |
| 候選 | `data.candidate` | 預估進度／預計行程進度 |
| 已完成 | `data.completed` | 實走紀錄／實走時間軸 |

已完成的行程若留下 GPS 紀錄，軌跡放在 `assets/tracks/<頁名>-<YYYY-MM-DD>.js`。

## 航點（waypoint）

行程中的一個地點，是 `schedule` 陣列的一個元素。欄位語意：

| 欄位 | 意思 |
|---|---|
| `loc` | 地點全名，畫面上顯示用 |
| `short` | **航點短名**——見下 |
| `pos` | 位置類型，同時決定顏色與 emoji。可用值見下 |
| `dist` | 自起點的**累計**里程（km），不是段長 |
| `ele` | 海拔（m） |
| `advice` | 給下次走的人的提示，**每個航點都必須有** |

### 航點短名

航點在**海拔圖 x 軸**上的顯示名稱。`loc` 常帶著標高或狀態註記
（`紗帽山 (H643m)`、`五峰旗停車場 (標高 65m)`、`捷運內湖站 (接駁)`），
那些在 x 軸上是雜訊。

預設由 `assets/detail.js` 的 `shortName()` 剝除；**剝不乾淨的航點在 `schedule` 裡直接給
`short`**。這是刻意的設計：例外用資料表達，不是每頁寫一個格式化函式——
2026-08-04 之前正是後者，18 頁有 8 種寫法在做同一件事。

### `pos` 的可用值與語意色

顏色由 `PaPaDetail.palette()` 決定，**跨頁一致**：

| 概念 | 顏色 | 說明 |
|---|---|---|
| 起訖點 | 該頁主色 `--accent` | 第一個與最後一個航點 |
| **最高點** | `#7c9e52` 山頂綠 | 見下方「最高點 vs 山頂」 |
| 一般航點 | `#a09080` | 其餘 |
| 第三種語意 | 各頁自訂 | 經 `palette({ extra })`，只開一層 |

其他跨頁共用的語意色（不屬於任何單頁的識別色）：
紅 `#ef4444` = 警示地形、藍 = 外部連結、琥珀 `amber-50/400/800` = 注意事項面板、
翡翠 `emerald-700` = 站徽與官方來源連結。

### 最高點 vs 山頂 vs 副峰

**「最高點」是全程海拔最高的那一個航點，一條路線只有一個。** 它有專屬的語意色，
因為讀者掃一眼海拔圖時最先找的就是它。

「山頂」「副峰」「稜線山頭」是路過的其他山頭，**不是最高點**，沒有專屬顏色。

2026-08-04 發現五頁沒有標示最高點：它們寫 `isPeak ? HIGH`，而 `isPeak` 的定義其實是
`wp.ele >= 950`——依海拔高低帶上色，最高點只是剛好落在高帶，與其他高點沒有區別。
`dinghu` 更是整條路線沒有任何航點標成「最高點」，它的最高處標的是「山頂」。
這是本檔存在的直接理由：**同一個概念在不同頁用不同的詞，顏色就跟著散掉。**

## 海拔軸地板（elevation floor）

海拔圖 y 軸的起點。市郊路線從 0 起讀得出爬升感；高山路線從 0 起會浪費半張圖，
所以設地板（`qixingshan` 與 `datunshan` 都設 450）。

**這是編輯判斷，不可推導**，所以是 `chart: { elevationFloor }` 旋鈕。
對照組是 y 軸上限：那個不給就讓 Chart.js 自己算，因為它的刻度演算法挑的數字
（1092→1100、155→160）不是固定百分比，硬推導反而會改掉畫面。
